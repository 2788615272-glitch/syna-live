# Contributing

1. Create a focused branch and keep changes scoped.
2. Run `npm test` and `npm run check` before opening a pull request.
3. Do not include real credentials, cookies, conversations, memories, logs,
   screenshots, or user-uploaded avatars.
4. New providers should be implemented as adapters behind the existing
   provider interface.
5. Keep the default experience usable without paid assets.
